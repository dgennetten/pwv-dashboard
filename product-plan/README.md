# PWV Insights — Design Handoff

This folder contains a portable package to implement **PWV Insights** from the Design OS project.

## What's Included

**Prompts**

- `prompts/one-shot-prompt.md` — Full-app implementation
- `prompts/section-prompt.md` — Per-section template (fill SECTION_NAME, SECTION_ID, NN)

**Instructions**

- `product-overview.md` — Always attach for context
- `instructions/one-shot-instructions.md` — All milestones in one file
- `instructions/incremental/` — `01-shell.md` … `04-leaderboards-trends.md`

**Assets**

- `design-system/` — Tokens, Tailwind color notes, fonts
- `data-shapes/` — README + `overview.ts` (aggregated contracts)
- `shell/components/` — `AppShell`, `MainNav`, `UserMenu`
- `sections/*/` — Components, `types.ts`, `sample-data.json`, `README.md`, `tests.md`

## Screenshots

No `product/**/*.png` files were present in the repo at export time. Add screenshots to each `sections/[id]/` folder if you capture them from Design OS.

## How to Use

**Incremental (recommended):** Shell first, then `activity-dashboard`, `trails`, `leaderboards-trends` using `section-prompt.md` with NN = 02–04.

**One-shot:** Use `one-shot-prompt.md` + `one-shot-instructions.md`.

## Zip

Regenerate root archive after changes:

```powershell
Compress-Archive -Path product-plan -DestinationPath product-plan.zip -Force
```

---

*Generated by Design OS export-product*
